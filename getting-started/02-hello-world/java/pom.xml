<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- Spring Boot parent gives us dependency management and the spring-boot:run goal. -->
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.4</version>
    <relativePath/>
  </parent>

  <groupId>org.pbinitiative.examples</groupId>
  <artifactId>hello-world-worker</artifactId>
  <version>1.0.0</version>

  <properties>
    <java.version>17</java.version>
    <!-- Set to the latest client release that matches your engine version:
         https://github.com/pbinitiative/zenbpm-java-client/releases -->
    <zenbpm.version>REPLACE_WITH_LATEST</zenbpm.version>
  </properties>

  <dependencies>
    <!-- ZenBPM Spring Boot starter: provides @JobWorker and auto-connects on startup.
         verify: coordinates/version against the zenbpm-java-client releases. -->
    <dependency>
      <groupId>org.zenbpm</groupId>
      <artifactId>zenbpm-spring-boot-starter</artifactId>
      <version>${zenbpm.version}</version>
    </dependency>
    <dependency>
      <groupId>org.zenbpm</groupId>
      <artifactId>zenbpm-client-core</artifactId>
      <version>${zenbpm.version}</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
