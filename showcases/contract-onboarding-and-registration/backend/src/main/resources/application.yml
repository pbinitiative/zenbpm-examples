server:
  port: 8081

zenbpm:
  base-url: ${ZENBPM_URL:http://zenbpm:8080/v1}

portal:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}
  process:
    default-key: ${PROCESS_KEY:my-process-key}

management:
  endpoints:
    web:
      exposure:
        include: health
